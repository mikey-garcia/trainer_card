/*
TODO:

- Add a workaround for desktop users...right now it's really ugly :/

- General Contact Info + Download contact info

- Add backend + db (probably Flask + ngrok) for users to create their own profiles?

*/



/* Get rid of scrolling for a nice 1 page feel -> do this later when can guarantee no need to scroll

var x=window.scrollX;
var y=window.scrollY;
window.onscroll=function(){window.scrollTo(x, y);};
*/